{
  "name": "GrowthContentBatch",
  "type": "object",
  "properties": {
    "batch_input_mode": {
      "type": "string",
      "enum": [
        "reviewed_evidence",
        "audited_seed_bootstrap"
      ],
      "description": "Trust input used to build the batch; older rows without this field use reviewed_evidence"
    },
    "batch_key": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Deterministic identity for one reviewed-evidence or audited-bootstrap input, content profile, and Phoenix target date"
    },
    "request_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Canonical hash of every generation input, including ordered seed and source lineage"
    },
    "parent_platform": {
      "type": "string",
      "enum": [
        "instagram",
        "tiktok"
      ]
    },
    "parent_campaign": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
    },
    "parent_content": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
    },
    "review_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Hash binding the exact evidence, decision, operator note, and review timestamps"
    },
    "review_schema_version": {
      "type": "string",
      "enum": [
        "growth-review.v2",
        "growth-review.v3"
      ],
      "description": "Reviewed-evidence contract; required by the server for reviewed_evidence batches"
    },
    "decision_policy_id": {
      "type": "string",
      "enum": [
        "growth-decision-sufficiency.v1"
      ]
    },
    "decision_policy_reason_codes": {
      "type": "array",
      "minItems": 2,
      "maxItems": 8,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "base_social_evidence_supported",
          "iterate_supported_by_social_evidence",
          "repeat_supported_by_exact_activation",
          "repeat_supported_by_mature_retention",
          "repeat_supported_by_paid_outcome",
          "repeat_supported_by_social_only_override",
          "hold_supported_by_three_comparable_snapshots"
        ]
      }
    },
    "decision_policy_evidence_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "comparable_fixed_age_snapshots": {
      "type": "integer",
      "minimum": 0,
      "maximum": 25000,
      "description": "Frozen server-counted comparison input copied from the reviewed parent"
    },
    "decision_override_note": {
      "type": "string",
      "minLength": 24,
      "maxLength": 500
    },
    "decision_override_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "evidence_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Canonical fixed-age social evidence hash used by the reviewed parent"
    },
    "conversion_evidence_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Canonical SHA-256 of the exact post-eligible funnel frozen with the reviewed parent"
    },
    "conversion_cutoff_at": {
      "type": "string",
      "format": "date-time",
      "description": "Report generation cutoff bound into this reviewed-evidence batch"
    },
    "review_decision": {
      "type": "string",
      "enum": [
        "repeat",
        "iterate"
      ]
    },
    "reviewed_at": {
      "type": "string",
      "format": "date-time"
    },
    "review_snapshot_captured_at": {
      "type": "string",
      "format": "date-time"
    },
    "bootstrap_policy_version": {
      "type": "string",
      "enum": [
        "audited-seed-bootstrap-v1"
      ]
    },
    "bootstrap_authorized_by": {
      "type": "string",
      "maxLength": 160
    },
    "bootstrap_authorized_at": {
      "type": "string",
      "format": "date-time"
    },
    "bootstrap_authorization_note": {
      "type": "string",
      "maxLength": 500
    },
    "bootstrap_authorization_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Hash binding the exact owner authorization, audited seed, selected donor lineage, and target date"
    },
    "target_date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Publishing date interpreted only in the persisted timezone"
    },
    "content_profile": {
      "type": "string",
      "enum": [
        "measured-next-batch-v1",
        "feature_explainer_video_v1"
      ],
      "description": "Server-owned generation and validation contract; older rows without this field use measured-next-batch-v1"
    },
    "timezone": {
      "type": "string",
      "enum": [
        "America/Phoenix"
      ]
    },
    "concept_count": {
      "type": "integer",
      "enum": [
        2,
        3
      ]
    },
    "slot_count": {
      "type": "integer",
      "enum": [
        2,
        3
      ]
    },
    "slot_keys": {
      "type": "array",
      "minItems": 2,
      "maxItems": 3,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "morning",
          "midday",
          "evening"
        ]
      }
    },
    "source_asset_keys": {
      "type": "array",
      "minItems": 2,
      "maxItems": 3,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
      }
    },
    "source_lineage": {
      "type": "array",
      "minItems": 2,
      "maxItems": 3,
      "items": {
        "type": "object",
        "properties": {
          "asset_key": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
          },
          "source_origin": {
            "type": "string",
            "enum": [
              "asset_pack",
              "repository_public"
            ]
          },
          "source_reference": {
            "type": "string",
            "maxLength": 300
          },
          "source_sha256": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          }
        },
        "required": [
          "asset_key",
          "source_origin",
          "source_reference",
          "source_sha256"
        ]
      }
    },
    "prompt_source_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Hash of the exact sanitized source summaries sent to the generator"
    },
    "generated_hook_reservations": {
      "type": "array",
      "minItems": 2,
      "maxItems": 6,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 120
      },
      "description": "Bounded generated hooks used for durable 28-day batch-to-batch deduplication"
    },
    "generated_hooks_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Canonical SHA-256 of the ordered generated hook reservations"
    },
    "seed_pack_batch_id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
    },
    "seed_pack_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "seed_concept_ids": {
      "type": "array",
      "minItems": 2,
      "maxItems": 3,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
      }
    },
    "seed_artifact_keys": {
      "type": "array",
      "minItems": 4,
      "maxItems": 6,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
      }
    },
    "seed_lineage": {
      "type": "array",
      "minItems": 2,
      "maxItems": 3,
      "items": {
        "type": "object",
        "properties": {
          "concept_id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
          },
          "source_asset_key": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
          },
          "instagram_artifact_key": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
          },
          "tiktok_artifact_key": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9._~-]{0,119}$"
          }
        },
        "required": [
          "concept_id",
          "source_asset_key",
          "instagram_artifact_key",
          "tiktok_artifact_key"
        ]
      }
    },
    "state": {
      "type": "string",
      "enum": [
        "generating",
        "ready",
        "render_authorized",
        "failed",
        "superseded",
        "revoked"
      ]
    },
    "pack_schema_version": {
      "type": "string",
      "enum": [
        "growth-render-pack.v1"
      ]
    },
    "pack_artifact_count": {
      "type": "integer",
      "minimum": 4,
      "maximum": 6
    },
    "canonical_pack_json": {
      "type": "string",
      "maxLength": 100000,
      "description": "Canonical serialized render pack; never source bytes, paths, credentials, or provider secrets"
    },
    "canonical_pack_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "lease_token": {
      "type": "string",
      "maxLength": 160
    },
    "lease_generation": {
      "type": "integer",
      "minimum": 0,
      "maximum": 1000000,
      "default": 0
    },
    "lease_acquired_at": {
      "type": "string",
      "format": "date-time"
    },
    "lease_expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "attempt_count": {
      "type": "integer",
      "minimum": 0,
      "maximum": 1000000,
      "default": 0
    },
    "requested_by": {
      "type": "string",
      "maxLength": 160
    },
    "requested_at": {
      "type": "string",
      "format": "date-time"
    },
    "state_changed_at": {
      "type": "string",
      "format": "date-time"
    },
    "ready_at": {
      "type": "string",
      "format": "date-time"
    },
    "render_authorized_by": {
      "type": "string",
      "maxLength": 160
    },
    "render_authorized_at": {
      "type": "string",
      "format": "date-time"
    },
    "render_authorization_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "render_authorization_note": {
      "type": "string",
      "maxLength": 500
    },
    "superseded_by_batch_key": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "superseded_at": {
      "type": "string",
      "format": "date-time"
    },
    "revoked_by": {
      "type": "string",
      "maxLength": 160
    },
    "revoked_at": {
      "type": "string",
      "format": "date-time"
    },
    "revocation_note": {
      "type": "string",
      "maxLength": 500
    },
    "failed_at": {
      "type": "string",
      "format": "date-time"
    },
    "last_error_code": {
      "type": "string",
      "maxLength": 120
    },
    "last_error_message": {
      "type": "string",
      "maxLength": 500
    }
  },
  "required": [
    "batch_key",
    "request_hash",
    "target_date",
    "timezone",
    "concept_count",
    "slot_count",
    "slot_keys",
    "source_asset_keys",
    "source_lineage",
    "prompt_source_sha256",
    "seed_pack_batch_id",
    "seed_pack_sha256",
    "seed_concept_ids",
    "seed_artifact_keys",
    "seed_lineage",
    "state",
    "lease_generation",
    "attempt_count",
    "requested_by",
    "requested_at",
    "state_changed_at"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "id": "__service_role_only__"
      }
    },
    "read": {
      "user_condition": {
        "id": "__service_role_only__"
      }
    },
    "update": {
      "user_condition": {
        "id": "__service_role_only__"
      }
    },
    "delete": {
      "user_condition": {
        "id": "__service_role_only__"
      }
    }
  }
}
